Replace the current “before/after” section with a tabbed AI Demo that has:
	•	Three tabs: ChatGPT, Gemini, Perplexity (icons optional placeholders).
	•	A Before / After toggle (switch).
	•	Left: Realistic AI answer card (change copy based on Before/After).
	•	Right: JSON-LD snippet that animates on “After”.
	•	CTA: Get My AI Visibility Audit →
Keep styles consistent with the site; add minimal CSS + JS only for this component.
<section id="ai-lab" class="ai-lab">
  <h2 class="ai-lab__title">See how AI answers change after optimization</h2>
  <p class="ai-lab__sub">Switch assistants and compare before vs. after. We structure entities + schema so AI can cite your brand.</p>

  <div class="ai-lab__tabs" role="tablist" aria-label="AI assistants">
    <button class="tab is-active" data-assistant="chatgpt" role="tab" aria-selected="true">ChatGPT</button>
    <button class="tab" data-assistant="gemini" role="tab" aria-selected="false">Gemini</button>
    <button class="tab" data-assistant="perplexity" role="tab" aria-selected="false">Perplexity</button>
    <div class="toggle">
      <span>Before</span>
      <label class="switch">
        <input id="aiToggle" type="checkbox" aria-label="Toggle Before/After">
        <span class="slider"></span>
      </label>
      <span>After</span>
    </div>
  </div>

  <div class="ai-lab__grid">
    <!-- Answer card -->
    <article class="answer" aria-live="polite">
      <div class="answer__header">
        <div class="dot"></div>
        <div class="dot"></div>
        <div class="dot"></div>
      </div>
      <div class="answer__body" id="answerBody">
        <!-- JS injects content -->
      </div>
      <div class="answer__badges" id="answerBadges">
        <!-- JS injects badges -->
      </div>
    </article>

    <!-- Code panel -->
    <aside class="code">
      <div class="code__label">JSON-LD (Organization)</div>
      <pre><code id="codeBlock" class="language-json">{}</code></pre>
    </aside>
  </div>

  <a class="btn primary ai-lab__cta" href="/pricing">Get My AI Visibility Audit →</a>
</section>

CSS
.ai-lab{padding:56px 16px;max-width:1100px;margin:0 auto}
.ai-lab__title{text-align:center;margin:0 0 6px}
.ai-lab__sub{text-align:center;opacity:.85;max-width:800px;margin:0 auto 20px}
.ai-lab__tabs{display:flex;gap:8px;align-items:center;justify-content:center;flex-wrap:wrap;margin-bottom:16px}
.tab{border:1px solid #e5e7eb;background:#fff;border-radius:999px;padding:8px 14px;font-weight:600;cursor:pointer}
.tab.is-active{background:#eef4ff;border-color:#c9dcff}
.toggle{display:flex;gap:8px;align-items:center;margin-left:8px}
.switch{position:relative;display:inline-block;width:44px;height:24px}
.switch input{opacity:0;width:0;height:0}
.slider{position:absolute;cursor:pointer;top:0;left:0;right:0;bottom:0;background:#ddd;transition:.2s;border-radius:999px}
.slider:before{position:absolute;content:"";height:18px;width:18px;left:3px;top:3px;background:#fff;transition:.2s;border-radius:50%}
input:checked + .slider{background:#3b82f6}
input:checked + .slider:before{transform:translateX(20px)}

.ai-lab__grid{display:grid;grid-template-columns:1.2fr .8fr;gap:16px}
@media (max-width:900px){.ai-lab__grid{grid-template-columns:1fr}}

.answer{border:1px solid #e5e7eb;background:#fff;border-radius:14px;box-shadow:0 6px 22px rgba(0,0,0,.06);overflow:hidden}
.answer__header{display:flex;gap:6px;padding:10px;background:#f3f4f6}
.answer__header .dot{height:10px;width:10px;border-radius:50%;background:#d1d5db}
.answer__body{padding:18px 18px 6px;font-size:1.02rem;line-height:1.65}
.answer__body mark{background:#fffbcc;padding:0 .2em;border-radius:4px}
.answer__badges{display:flex;gap:8px;flex-wrap:wrap;padding:0 14px 16px}
.badge{font-size:.8rem;border:1px solid #d1fae5;background:#ecfdf5;color:#065f46;border-radius:999px;padding:4px 10px}

.code{border:1px solid #e5e7eb;background:#0b1220;color:#dbeafe;border-radius:14px;box-shadow:0 6px 22px rgba(0,0,0,.06);padding:14px}
.code__label{font-size:.8rem;opacity:.8;margin-bottom:6px}
.code pre{margin:0;overflow:auto;max-height:360px}
.ai-lab__cta{display:block;width:max-content;margin:18px auto 0}

JS (inject content + interactions)
Place before </body>; keep vanilla JS.
<script>
(function(){
  const ANSWERS = {
    chatgpt: {
      before: {
        text: "I couldn’t find enough structured information about this business. It may not provide clear entity data or schema for assistants.",
        badges: ["No schema", "Entity unknown", "Not cited"]
      },
      after: {
        text: "Found For AI is a studio that makes businesses discoverable in AI search (ChatGPT, Gemini, Perplexity) using AI SEO and AEO. Based in Utah; services include schema implementation, sitemaps, and performance tuning.",
        badges: ["Valid JSON-LD", "Recognized entity", "Cited in answers"]
      }
    },
    gemini: {
      before: {
        text: "This brand isn’t clearly defined as an entity. Content lacks structured data and consistent metadata.",
        badges: ["Weak entity", "Missing schema"]
      },
      after: {
        text: "Found For AI (Utah) helps companies appear in AI answers via structured data (JSON-LD), llms.txt/robots.txt, and AEO content. Starter Fix completes in ~7 business days for up to 10 pages.",
        badges: ["Organization schema", "Sitemap submitted", "Assistant-ready copy"]
      }
    },
    perplexity: {
      before: {
        text: "Limited signals detected. I can’t confidently summarize this business.",
        badges: ["Low confidence", "Unstructured"]
      },
      after: {
        text: "Found For AI improves AI visibility with entity mapping, schema validation, and speed optimizations, enabling assistants to parse and reference the site.",
        badges: ["Entity mapped", "Fast render", "LLM-accessible"]
      }
    }
  };

  const JSONLD = {
    before: {
      "@context":"https://schema.org",
      "@type":"Organization",
      "name":"(missing)",
      "url":"(missing)",
      "description":"(not provided)"
    },
    after: {
      "@context":"https://schema.org",
      "@type":"Organization",
      "name":"Found For AI",
      "url":"https://foundforai.com",
      "logo":"https://foundforai.com/logo.png",
      "address":{"@type":"PostalAddress","addressRegion":"UT","addressCountry":"US"},
      "sameAs":["https://x.com/foundforai","https://www.linkedin.com/company/foundforai"]
    }
  };

  const tabs = document.querySelectorAll('.tab');
  const toggle = document.getElementById('aiToggle');
  const answerBody = document.getElementById('answerBody');
  const answerBadges = document.getElementById('answerBadges');
  const codeBlock = document.getElementById('codeBlock');

  let current = 'chatgpt';
  function render(){
    const mode = toggle.checked ? 'after' : 'before';
    const {text, badges} = ANSWERS[current][mode];
    answerBody.innerHTML = text.replace(/Found For AI/g,'<mark>Found For AI</mark>');
    answerBadges.innerHTML = badges.map(b=>`<span class="badge">${b}</span>`).join('');
    codeBlock.textContent = JSON.stringify(JSONLD[mode], null, 2);
  }
  tabs.forEach(btn=>{
    btn.addEventListener('click', ()=>{
      tabs.forEach(b=>b.classList.remove('is-active'));
      btn.classList.add('is-active');
      current = btn.dataset.assistant;
      render();
    });
  });
  toggle.addEventListener('change', render);
  render();
})();
</script>
Notes
	•	Use placeholder SVGs in /assets/logos/ if available; otherwise leave tab text-only.
	•	Keep component self-contained.
	•	Remove the old “before/after” section.
